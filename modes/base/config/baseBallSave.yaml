#config_version=5

event_player:
 ball_save_initial_saving_ball:
   invoke_notify_by_event:
     new_text: "LOST\nBALL"
     new_value: "SAVED"
 in_mode_ball_save:
   invoke_notify_by_event:
     new_text: "MODE FAIL\nBALL"
     new_value: "SAVED"

queue_event_player:
  ball_save_initial_enabled:
    queue_event: ball_save_queued
    events_when_finished: ball_save_timer_start

queue_relay_player:
  ball_save_queued:
    post: waiting_for_eject_success
    wait_for: sw_playfield_active

#ball_saves:
# initial:
#   active_time: 8s
#   grace_period: 2s
#   enable_events: mode_base_started
#   timer_start_events: ball_save_timer_start
#   early_ball_save_events: s_rooutlaneright_active, s_rooutlaneleft_active
#   auto_launch: false
#   balls_to_save: 1

